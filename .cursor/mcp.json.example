{
  "mcpServers": {
    "mcp-server-for-revit-local": {
      "command": "C:/Program Files/nodejs/node.exe",
      "args": [
        "<REPO_ROOT>/server/build/index.js"
      ],
      "cwd": "<REPO_ROOT>/server",
      "env": {
        "MCP_TOOL_PROFILE": "default"
      }
    }
  }
}
