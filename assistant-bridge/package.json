{
  "name": "revit-assistant-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local bridge: Revit in-panel chat → Cursor SDK agent → Revit MCP",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js",
    "test": "npm run build && node --test \"dist/**/*.test.js\""
  },
  "dependencies": {
    "@cursor/sdk": "^1.0.24"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.8.2"
  }
}
